{
  "name": "fees-management-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "node backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
